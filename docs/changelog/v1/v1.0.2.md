# Slothlet Vine v1.0.2 Changelog

**Release Date**: September 2026
**Release Type**: Patch
**Branch**: `next` → `master`

---

## Overview

A dependency-only patch. No runtime code changed — the v1.0.0 surface, wire protocol, and every transport are untouched. Backfilled changelog for a release that shipped without one.

---

## 🔧 Dependencies

- **dev:** `@cldmv/slothlet` bumped from `3.15.0` to `3.15.1` (#15). This is the dev/test pin only; the **peer** floor stays `>=3.14.0` and nothing at runtime requires the newer version.

---

## Upgrade notes

A drop-in for v1.0.1 — nothing to do. The change is confined to the development toolchain.
